# 🛠️ AInote Server - Installation Guide

คู่มือสำหรับการติดตั้งและเตรียมระบบ Server (Backend) สำหรับนักพัฒนาหรือ Admin

## 📋 ความต้องการระบบ (Prerequisites)
1. **OS:** Windows 10/11
2. **Python:** เวอร์ชัน 3.10 หรือใหม่กว่า (แนะนำ 3.12)
3. **Visual Studio Build Tools:** จำเป็นสำหรับการคอมไพล์ `llama-cpp-python`
4. **Git:** สำหรับดึงโค้ดโปรเจกต์
5. **Node.js:** สำหรับรัน Frontend (Angular)

---

## 🚀 ขั้นตอนการติดตั้ง (Step-by-Step)

### 1. เตรียมเครื่องมือ C++ (สำคัญมาก)
โปรแกรมต้องใช้ `llama-cpp-python` ซึ่งต้องมีการคอมไพล์ C++
1. โหลด **Visual Studio Build Tools** (จากเว็บ Microsoft)
2. ตอนติดตั้งในแท็บ "Workloads" ให้ติ๊กถูกหน้า **"Desktop development with C++"**
3. กด Install และรอจนเสร็จ (ใช้เวลาพอสมควร)

### 2. เตรียม Python Environment
เปิด Terminal (`cmd` หรือ `powershell`) ในโฟลเดอร์ `backend`:

```bash
# อัพเกรด pip
python -m pip install --upgrade pip

# ติดตั้ง Library ทั้งหมด
pip install -r requirements.txt

# ติดตั้ง OpenAI Library (สำคัญสำหรับ Cloud Brain)
pip install openai
```

### 3. ตั้งค่าการเชื่อมต่อ (File: .env)
แก้ไขไฟล์ `.env` ในโฟลเดอร์ `backend` ให้เป็นดังนี้:

```ini
# --- เลือกโหมดการทำงาน ---
# local = ใช้ CPU เครื่อง (ช้าแต่ฟรี)
# colab = ใช้ Google Colab GPU (เร็วมาก)
LLM_PROVIDER=colab

# --- ตั้งค่า Local (ถ้าใช้) ---
LOCAL_MODEL_PATH=./models/ThaiLLM-8B-Instruct.Q8_0.gguf

# --- ตั้งค่า Cloud Brain (Colab) ---
# เอารหัสจาก Ngrok ที่ได้จาก Colab มาใส่ที่นี่
OPENAI_BASE_URL=https://xxxx-xx-xx-xx-xx.ngrok-free.app/v1
OPENAI_API_KEY=not-needed
```

---

## ☁️ วิธีติดตั้ง Google Colab (Remote Brain)

1. อัปโหลดไฟล์โมเดล `.gguf` ไปที่ Google Drive
   (สร้างโฟลเดอร์ชื่อ `Model` แล้วเอาไฟล์ไปวาง)
2. เปิดไฟล์ `colab_api.ipynb` ใน Google Colab
3. ไปที่เว็บ [Ngrok Dashboard](https://dashboard.ngrok.com) เพื่อเอา **Authtoken**
4. รันสคริปต์ใน Colab ทีละขั้นตอน
5. ก๊อปปี้ลิ้งก์ที่ลงท้ายด้วย `.ngrok-free.app` มาใส่ใน `.env`

---

## ⚠️ ปัญหาที่พบบ่อย (Troubleshooting)

**Q: Error "No module named 'openai'"**
A: พิมพ์คำสั่ง `pip install openai` ใน Terminal แล้วรัน Server ใหม่

**Q: Error "failed to build installable wheels for llama-cpp-python"**
A: แสดงว่ายังไม่ได้ลง Visual Studio Build Tools (ทำตามข้อ 1 ใหม่)

**Q: น้องมะลิพูดภาษาอังกฤษ หรือมีคำว่า <think> ติดมา**
A: ให้ Restart Server (Backend) ใหม่ครับ ผมอัพเดทตัวกรองให้แล้ว

**Q: Colab ขึ้น Error 404/Connection Refused**
A: 
1. เช็คว่าเปิด Colab ทิ้งไว้หรือไม่ (ถ้าปิดแท็บ Server จะดับ)
2. เช็คว่า Ngrok URL เปลี่ยนหรือไม่ (ทุกครั้งที่รันใหม่ URL จะเปลี่ยน ต้องมาแก้ใน .env เสมอ)

---
*Created by Antigravity (Assistant)* 🤖✨
